general:
    artifacts:
        - "reports"

machine:
    node:
        version: 0.12.9
    environment:
        CIRCLE_TEST_REPORTS: reports

dependencies:
    pre:
        - npm install -g grunt-cli

test:
    override:
        - grunt circle
